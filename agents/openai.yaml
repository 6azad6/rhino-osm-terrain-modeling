interface:
  display_name: "Rhino OSM Terrain Studio"
  short_description: "Preview OSM sites and generate materialized .3dm terrain"
  default_prompt: "Use $rhino-osm-terrain-modeling to select and preview a site, configure layers and building heights, acquire OSM and DEM data, and generate a verified materialized .3dm without opening Rhino."
