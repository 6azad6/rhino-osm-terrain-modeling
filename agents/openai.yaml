interface:
  display_name: "Rhino OSM Terrain Studio"
  short_description: "Select sites and build Rhino terrain from OSM and DEM"
  default_prompt: "Use $rhino-osm-terrain-modeling to select a site, acquire OSM and DEM data, and build a verified Rhino 8 model."
